# Production build-time env vars (read by Vite during `vite build`)
# NOTE: VITE_DASHBOARD_SECRET is NOT a real secret — it is embedded in the
# JS bundle and visible to anyone with DevTools. It acts as an accidental-write
# guard, not a security boundary. Must match DASHBOARD_SECRET in the server .env.
VITE_DASHBOARD_SECRET=d812b4a1f7d84e4990f2a36a2e2cfa2e
