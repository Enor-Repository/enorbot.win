# Dashboard Frontend Environment Variables
# Copy this to .env.local for local development

# API Base URL - DO NOT hardcode localhost in code!
VITE_API_URL=http://localhost:3004

# Dashboard API auth secret (must match DASHBOARD_SECRET in backend .env)
# If not set, write requests are sent without auth header
VITE_DASHBOARD_SECRET=change-me-in-production
